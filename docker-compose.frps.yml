services:
  frps:
    image: ghcr.io/alexcdever/frps:latest
    container_name: frps
    restart: unless-stopped
    ports:
      - "7000:7000"
      - "7500:7500"
    volumes:
      - ./config/frps.toml:/etc/frp/frps.toml:ro
